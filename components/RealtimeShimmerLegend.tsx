"use client";

/**
 * Formerly auto-showed an educational "Linh khí mới" banner on chapter_update.
 * That stacked with RealtimeToastHost / ReaderChapterFreshHint for the same event.
 * Card shimmer (useFreshStoryRealtime) still runs; announcements stay single-surface.
 */
export function RealtimeShimmerLegend() {
  return null;
}
